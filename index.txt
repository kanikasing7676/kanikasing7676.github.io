<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IONOS Premium Domain Project - Mockup Hub</title>
    <style>
        /* --- BRANDING VARIABLES --- */
        :root {
            --ionos-blue: #003d8f;
            --ionos-dark-blue: #002966;
            --ionos-light-blue: #00b0f0; 
            --text-main: #1f2d3d;
            --bg-gray: #f4f7f9;
        }

        body {
            font-family: "Segoe UI", Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--bg-gray);
            color: var(--text-main);
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* HEADER */
        header {
            background-color: #fff;
            border-bottom: 1px solid #dbe2e8;
            padding: 20px 40px;
        }
        .logo {
            font-size: 26px;
            font-weight: 800;
            color: var(--ionos-blue);
            letter-spacing: -0.5px;
            text-decoration: none;
        }

        /* MAIN CONTENT */
        main {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding: 60px 20px;
        }

        h1 {
            color: var(--ionos-blue);
            margin-bottom: 10px;
            font-weight: 300;
            font-size: 36px;
        }

        .subtitle {
            color: #666;
            margin-bottom: 50px;
            font-size: 18px;
        }

        /* CARD GRID */
        .card-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 30px;
            max-width: 800px;
            width: 100%;
        }

        .card {
            background: white;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
            border: 1px solid #e0e0e0;
            transition: transform 0.2s, box-shadow 0.2s;
            text-align: left;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 25px rgba(0,0,0,0.1);
            border-color: var(--ionos-light-blue);
        }

        .card h2 {
            font-size: 20px;
            margin-top: 0;
            color: var(--ionos-dark-blue);
        }

        .card p {
            font-size: 14px;
            color: #666;
            line-height: 1.5;
            margin-bottom: 20px;
            height: 60px; /* Uniform height for text */
        }

        .btn {
            display: block;
            width: 100%;
            padding: 12px;
            text-align: center;
            background-color: var(--ionos-light-blue);
            color: white;
            text-decoration: none;
            font-weight: 700;
            border-radius: 4px;
            transition: background 0.3s;
        }

        .btn:hover {
            background-color: #009ecc;
        }

        /* STATUS BADGES */
        .badge {
            font-size: 10px;
            text-transform: uppercase;
            font-weight: bold;
            padding: 4px 8px;
            border-radius: 4px;
            margin-bottom: 15px;
            display: inline-block;
        }
        .badge-grid { background: #e6f2ff; color: var(--ionos-blue); }
        .badge-single { background: #fff4e5; color: #b05500; }

        footer {
            text-align: center;
            padding: 20px;
            font-size: 12px;
            color: #999;
            border-top: 1px solid #eee;
        }
    </style>
</head>
<body>

    <header>
        <a href="#" class="logo">IONOS</a>
    </header>

    <main>
        <h1>Premium Domain UX Project</h1>
        <p class="subtitle">Review the proposed workflows for Registry Premium & Aftermarket integration.</p>

        <div class="card-container">
            
            <div class="card">
                <span class="badge badge-single">Scenario A</span>
                <h2>Single Search Result</h2>
                <p>Detailed view for a specific premium query (e.g., travel-deals.shop). Features the new "One-Time Fee" tooltip, "Blue Label" cart interaction, and strict T&C dropdowns.</p>
                <a href="newsinglesearch.html" class="btn">View Single Mockup &rarr;</a>
            </div>

            <div class="card">
                <span class="badge badge-grid">Scenario B</span>
                <h2>Comparative Grid Search</h2>
                <p>Comparison view showing Registry Premium vs. Aftermarket options side-by-side. Highlights the difference in pricing models (High Renewal vs. One-Time Fee).</p>
                <a href="genericsearchnew.html" class="btn">View Grid Mockup &rarr;</a>
            </div>

        </div>
    </main>

    <footer>
        &copy; 2024 IONOS Mockup Project. Internal Use Only.
    </footer>

</body>
</html>